# MongoDB connection string — e.g. from MongoDB Atlas (free tier works):
# mongodb+srv://<user>:<password>@<cluster>.mongodb.net/flow
MONGODB_URI=

# Any long random string; signs the auth tokens.
JWT_SECRET=

# Optional, defaults to 3000.
PORT=3000
