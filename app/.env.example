# URL of the Flow sync server (server/ folder).
# Dev on the same Wi-Fi: http://<your-PC-LAN-IP>:3000  (NOT localhost — the phone can't reach it)
# Production: your Railway/hosting URL, e.g. https://flow-server.up.railway.app
# Restart `npx expo start` after changing this.
EXPO_PUBLIC_API_URL=
